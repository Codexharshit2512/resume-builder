import React from "react";
import { use } from "react-router-dom";

const ResumeWrapper = (props) => {
  const routes = ["contact", "skills", "education", "experiance"];
  const history = useHistory;
  return <div></div>;
};

export default ResumeWrapper;
